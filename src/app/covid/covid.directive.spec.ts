import { CovidDirective } from './covid.directive';

describe('CovidDirective', () => {
  it('should create an instance', () => {
    const directive = new CovidDirective();
    expect(directive).toBeTruthy();
  });
});
